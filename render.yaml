services:
  - type: web
    name: disease-prediction-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app_api:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    autoDeploy: true
